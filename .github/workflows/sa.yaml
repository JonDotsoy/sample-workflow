name: demo release

on: push

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

jobs:
  release:
    uses: JonDotsoy/sample-workflow/.github/workflows/npm-release.yml@demo-v1
    with:
      branch: feature/javascript-release
